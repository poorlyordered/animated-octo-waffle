# Quickstart: M38 Decision Saved Views

1. Run targeted unit tests:
   - `npm test -- decision-list-filters`
2. Run browser smoke:
   - `npm run test:e2e -- command-surfaces.spec.ts`
3. Run the full local gate:
   - `npm run typecheck`
   - `npm run lint`
   - `npm test`
   - `npm run build`

